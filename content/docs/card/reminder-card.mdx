---
title: Reminder Card
description: Reminder card that shows action on hover
labels: ["requires interaction", "hover"]
author: harimanok_
---

<ComponentPreview name="card-reminder-card--docs" />

1. Install dependencies
   ```bash
   yarn add lucide-react
   ```

# Installation

2. Create a new file called `reminder-card.tsx` inside the `components/animata/card` directory.

macOS/Linux:

```bash
mkdir -p components/animata/card && touch components/animata/card/reminder-card.tsx
```

windows:

```bash
mkdir components\animata\card && type null > components\animata\card\reminder-card.tsx
```

3. Open the newly created file and add the following code:

```jsx file=<rootDir>/animata/card/reminder-card.tsx

```

# Credits

Built by [Hari Lamichhane](https://github.com/hari)
