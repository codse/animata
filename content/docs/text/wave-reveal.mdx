---
title: Wave Reveal
description: Reveal letter or word one by one with a wave effect & optional blur effect — built with Tailwind CSS for headings, hero copy, and attention-grabbing labels.
---

<ComponentPreview name="text-wave-reveal--primary" />

## Installation

### CLI

<RegistryInstall category="text" name="wave-reveal" />

### Manual

<Steps>

<Step>Add to your CSS</Step>
```css
@keyframes reveal-up {
  0% { opacity: 0; transform: translateY(80%); }
  100% { opacity: 1; transform: translateY(0); }
}
@keyframes reveal-down {
  0% { opacity: 0; transform: translateY(-80%); }
  100% { opacity: 1; transform: translateY(0); }
}
@keyframes content-blur {
  0% { filter: blur(0.3rem); }
  100% { filter: blur(0); }
}
@theme {
  --ease-minor-spring: cubic-bezier(0.18, 0.89, 0.82, 1.04);
}
```

<Step>Run the following command</Step>

It will create a new file called `wave-reveal.tsx` inside the `components/animata/text` directory.

```bash
touch components/animata/text/wave-reveal.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/animata/text/wave-reveal.tsx

```

</Steps>

## Credits

Built by [hari](https://github.com/hari)
