---
title: Kinetic Center Build
description: A word appears in the center; each new word enters from right to left with a soft blur and pushes the existing line until the full phrase locks centered.
author: pixelpoint
---

<ComponentPreview name="text-kinetic-center-build--primary" />

## Installation

### CLI

<RegistryInstall category="text" name="kinetic-center-build" />

### Manual

<Steps>

<Step>Install the shared runtime</Step>

Every preset in the text category shares its WAAPI loop + scoped stage
styles with the same `text-animator` runtime. Skip this step if you
already added it for another text preset.

```bash
mkdir -p components/animata/text && touch components/animata/text/text-animator.tsx components/animata/text/text-animator.css
```

Paste the runtime into `text-animator.tsx`:

```tsx file=<rootDir>/animata/text/text-animator.tsx

```

And the styles into `text-animator.css`:

```css file=<rootDir>/animata/text/text-animator.css

```

<Step>Run the following command</Step>

It will create a new file called `kinetic-center-build.tsx` inside the `components/animata/text` directory.

```bash
mkdir -p components/animata/text && touch components/animata/text/kinetic-center-build.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/animata/text/kinetic-center-build.tsx

```

</Steps>

## Credits

Based on [pixel-point/animate-text](https://github.com/pixel-point/animate-text)
