---
title: Reveal image of list item
description: Reveals the images on hover.
labels: ["needs interaction", "hover"]
---

<ComponentPreview name="list-item-reveal-image-list-item--docs" />

# Installation

1. Create a new file called `how-image-list-item.tsx` inside the `components/animata/list-item` directory.

```bash
touch components/animata/list-item/reveal-image-list-item.tsx
```

2. Open the newly created file and add the following code:

```jsx file=<rootDir>/animata/list-item/reveal-image-list-item.tsx

```
