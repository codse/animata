---
title: Gooey Tabs
description: Icon tabs that merge with an SVG goo filter when one label expands to show its title.
labels: ["requires interaction", "click"]
author: harimanok_
---

<ComponentPreview name="tabs-gooey-tabs--primary" />

## Installation

### CLI

<RegistryInstall category="tabs" name="gooey-tabs" />

### Manual

<Steps>
<Step>Install dependencies</Step>

```bash
npm install motion @phosphor-icons/react
```

<Step>Run the following command</Step>

It will create a new file called `gooey-tabs.tsx` inside the `components/animata/tabs` directory.

```bash
mkdir -p components/animata/tabs && touch components/animata/tabs/gooey-tabs.tsx
```

<Step>Paste the code</Step>

Open the newly created file and paste the following code:

```jsx file=<rootDir>/animata/tabs/gooey-tabs.tsx

```

</Steps>

## Usage

Compose tabs with primitives. Each `GooeyTabs.Tab` needs a `color` class string; put icons and labels inside.

```tsx
import GooeyTabs from "@/animata/tabs/gooey-tabs";
import { HouseIcon } from "@phosphor-icons/react";

<GooeyTabs defaultActiveIndex={0}>
  <GooeyTabs.List>
    <GooeyTabs.Tab color="bg-violet-500 hover:bg-violet-600" label="Home">
      <GooeyTabs.Icon>
        <HouseIcon weight="duotone" aria-hidden />
      </GooeyTabs.Icon>
      <GooeyTabs.Label>Home</GooeyTabs.Label>
    </GooeyTabs.Tab>
  </GooeyTabs.List>
</GooeyTabs>
```

Use `defaultActiveIndex={-1}` for an all-icon row. Optional `intensity`, `contrast`, and `lightness` on the root adjust the SVG goo filter. Width is driven by CSS grid (`0fr` / `1fr` on the label column) — no layout measurement in JavaScript.

## Credits

Built by [hari](https://github.com/hari) · [@harimanok_](https://x.com/harimanok_)

Inspired by [@wickedmishra](https://x.com/wickedmishra) on Twitter
