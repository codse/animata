---
title: Security Alert
description: This widget indicates theres a security alert in the system. It shows the number of issues the system is facing and how risky the issue is.
---

<ComponentPreview name="widget-security-alert--docs" />

# Installation

1. Create a new file called `security-alert.tsx` inside the `components/animata/widget` directory.

   macOS/Linux:

   ```bash
   mkdir -p components/animata/widget && touch components/animata/widget/security-alert.tsx
   ```

   windows:

   ```bash
   mkdir components\animata\widget && type null > components\animata\widget\security-alert.tsx
   ```

2. Open the newly created file and add the following code:

```jsx file=<rootDir>/animata/widget/security-alert.tsx

```
