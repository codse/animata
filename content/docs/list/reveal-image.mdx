---
title: Reveal images
description: Reveals the images on hover.
labels: ["needs interaction", "hover"]
---

<ComponentPreview name="list-reveal-image--docs" />

# Installation

1. Create a new file called `reveal-image.tsx` inside the `components/animata/list` directory.

```bash
touch components/animata/list/reveal-image.tsx
```

2. Open the newly created file and add the following code:

```jsx file=<rootDir>/animata/list/reveal-image.tsx

```
