---
title: Social Media Icon List
description: Displays a list of social media icons that change color when hovered
labels: ["requires interaction", "hover"]
author: @RBChiranjibi
---

<ComponentPreview name="list-social-media-icon-list--docs" />

# Installation

1. Install dependencies
   ```bash
   yarn add lucide-react
   ```
2. Create a new file called `social-media-icon-list.tsx` inside the `components/animata/list` directory.

   macOS/Linux:

   ```bash
   mkdir -p components/animata/list && touch components/animata/list/social-media-icon-list.tsx
   ```

   windows:

   ```bash
   mkdir components\animata\list && type null > components\animata\list\social-media-icon-list.tsx
   ```

3. Open the newly created file and add the following code:

   ```jsx file=<rootDir>/animata/list/social-media-icon-list.tsx

   ```

# Credits

Built by [Chiranjibi Ranabhat](https://github.com/chiranjibi10)
