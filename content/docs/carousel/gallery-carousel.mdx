---
title: Gallery Carousel
description: a nice gallery carousel with modern and elegant look
author: YourTwitterUsername
---

<ComponentPreview name="carousel-gallery-carousel--docs" />

## Installation

<Steps>
<Step>Install dependencies</Step>

```bash
npm install framer-motion lucide-react
```

<Step>Update `tailwind.config.js`</Step>

Add the following to your tailwind.config.js file.

```json
module.exports = {
  theme: {
    extend: {
    }
  }
}
```

<Step>Run the following command</Step>

It will create a new file `gallery-carousel.tsx` inside the `components/animata/carousel` directory.

```bash
mkdir -p components/animata/carousel && touch components/animata/carousel/gallery-carousel.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx file=<rootDir>/animata/carousel/gallery-carousel.tsx

```

</Steps>

## Credits

Built by [Ajay Singh](https://github.com/Ajay01103)
