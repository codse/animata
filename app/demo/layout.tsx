import type { ReactNode } from "react";

/** Shell is typographically neutral — each demo owns fonts and palette. */
export default function DemoLayout({ children }: { children: ReactNode }) {
  return children;
}
